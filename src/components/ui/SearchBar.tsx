import React from "react";
import { Search } from "lucide-react";

const SearchBar = () => {
  return (
    <div>
      <Search className="w-5 h-5 hover:text-shop-dark-red hoverEffect" />
    </div>
  );
};

export default SearchBar;
